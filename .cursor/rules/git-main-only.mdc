---
description: Git 工作流：只用 main；禁止私自创建 cursor/ 等功能分支（除非用户明文要求）
alwaysApply: true
---

# Git：仅 main（仓库默认工作流）

维护者与 Cursor Agent **默认只在 `main` 上开发与推送**。云端部署（如 Vercel）与本地调试端口分别使用即可，**不需要**为常规任务新建长期功能分支。

## 必须遵守

1. **禁止**在未收到用户**明确、单独一句**指令的情况下：
   - `git checkout -b …` / 新建任意功能分支（含 `cursor/`、`feat/`、`fix/` 等）
   - `git push -u origin <非 main 分支>`
   - 建议或默认把改动推到非 `main` 分支

2. **默认流程**（执行任务前若尚未在 main，应先说明再执行）：
   - `git checkout main`
   - `git pull origin main`（若远程可用）
   - 在 **main** 上修改、提交、`git push origin main`

3. 若当前仓库已在其它分支上，**应先询问用户是否合并/切回 main**，除非用户在本轮对话里已经明确要求留在该分支。

4. 用户若明确说「开分支 / PR / 不要直接推 main」时，**仅此轮**可以偏离本规则。

## 说明

- 本规则约束 **Cursor Agent 行为**，**不能**在操作系统层面禁止人工或其它工具执行 `git branch`。
- 若历史遗留远程分支需清理，仅在用户要求时协助删除；日常不把新建分支当作默认选项。
