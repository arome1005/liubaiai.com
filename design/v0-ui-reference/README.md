# V0 UI 设计参照（独立原型）

本目录为 **v0 导出的 Next.js 界面原型**，仅供视觉与交互参照；与仓库根目录 **Vite + React 主应用**（`src/`）**无代码耦合**。

- 主站开发与构建：`npm run build` / `npm run dev`（不依赖本目录）
- 在本目录内单独预览（可选）：`npm install && npm run dev`
- 后续主站 UI 迭代时，以本目录为参考逐步对齐组件与样式

请勿在根目录 `package.json` 或 `src/` 中 import 本路径下的文件。
