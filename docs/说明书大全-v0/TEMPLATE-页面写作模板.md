# 页面说明模板（v0 目标态）

> 本模板用于“最终目标态说明”，默认 UI 已对齐 v0 且功能全部可用。

---

## 1. 页面定位

- **页面名**：
- **路由**：
- **一句话目的**：
- **主要用户任务（3–7 条）**：

---

## 2. 入口与导航

- **从哪里进入本页**：
- **从本页可以去哪里**：
- **返回/后退行为**：
- **深链参数/状态保留规则**：
- **快捷键（如有）**：

---

## 3. 信息架构（v0 目标 UI）

按 v0 的真实结构，从上到下、从左到右列出：

- **顶部区域**：
- **左侧栏（如有）**：
- **中间主区域**：
- **右侧栏（如有）**：
- **底部/浮层（如有）**：

并对每个区块补充：

- **显示条件**（什么时候出现/消失）
- **默认状态**
- **交互入口**（按钮/菜单/快捷键/手势）

---

## 4. 功能清单（角落级）

把本页所有功能拆成“原子动作”，逐条写清：

- **动作名**：点击/输入/拖拽/批量/快捷键
  - **触发位置**：
  - **前置条件**：
  - **执行过程**：
  - **结果与反馈**：
  - **失败与提示**：

---

## 5. 数据与写回（读/写/缓存）

- **数据读取**：读什么、从哪里读、何时刷新
- **数据写回**：写什么、写到哪里、是否需要确认/事务
- **并发/冲突**：同一数据多处修改时的规则
- **缓存**：本页有哪些缓存、失效策略

---

## 6. 跨模块联动

### 6.1 本页触发 → 影响其它页

- **触发点**：
  - **落点页面**：
  - **落点表现**：
  - **联动来源**（依赖的数据/能力来自哪）：

### 6.2 其它页触发 → 本页体现

- **触发点（来自其它页）**：
  - **本页表现**：
  - **来源**：

---

## 7. 空态 / 加载态 / 错误态

- **空态清单**：无数据/无权限/未选择上下文等
- **加载态清单**：骨架屏/按钮 loading/阻塞与非阻塞
- **错误态清单**：网络失败/写回失败/校验失败/配额不足
- **自救建议**：用户如何恢复/回滚/重试

---

## 8. 验收与手测脚本

提供可执行步骤（10–30 条）：

- **基础路径**：
- **边界路径**：
- **错误路径**：