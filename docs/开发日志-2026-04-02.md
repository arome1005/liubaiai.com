# 开发日志（2026-04-02）

## 背景

目标：大改首页/导航 + 写作页全屏壳；并开始搭建“注册/登录/云端数据存储（Postgres）”的后端底座。

---

## 已完成（Done）

### 1) UI / 路由 / 壳子

- 新增首页：`/` → `HomePage`（欢迎区 + 七入口 + 最近作品）。
- 作品库路径：`/library`（原先 `/`）。
- 顶部居中七导航（留白/推演/流光/问策/落笔/生辉/藏经），设置改为右上角齿轮。
- 写作编辑页改为独立全屏壳：新增 `EditorShell`，`/work/:workId` 走 `EditorShell`（无七导航）。
- 生辉与写作解耦：`/sheng-hui` → `ShengHuiPage` 占位（顶栏“生辉”指向这里）。
- AI 右侧栏仅随写作：`AppShell` 已移除全局右栏；`EditorShell` 保留右栏（AI/概要/圣经/参考）。
- 修复：去掉了一个旧 CSS（≤720px）里对 `.ai-panel { display:none }` 的全局隐藏，避免窄屏右栏空白。

### 2) 数据库（VPS Postgres）

- 本机已成功连接到你的 VPS Postgres（注意：该 Postgres 不支持 SSL，因此连接需 `DB_SSL=false`）。
- 已创建项目所需的基础表（works/chapters/volumes/bible/reference 等）：
  - 迁移脚本：`backend/migrate.js`
  - 连接测试脚本：`backend/test-connection.js`
  - 命令：`npm run db:test`、`npm run db:migrate`

### 3) 后端 API（已写代码，但还没完成跑通闭环）

- 已添加最小后端 API：`backend/server.js`
  - 注册：`POST /api/auth/register`
  - 登录：`POST /api/auth/login`
  - 当前用户：`GET /api/auth/me`
  - 作品：`GET/POST/PATCH/DELETE /api/works`
  - 章节：`GET /api/works/:workId/chapters`、`POST /api/works/:workId/chapters`、`PATCH /api/chapters/:id`、`POST /api/works/:workId/chapters/reorder`
- 开发代理：`vite.config.ts` 已加 `/api` → `http://localhost:8787`（后续需根据实际端口调整）。

---

## 当前未完成（Todo / Blocked）

### A) 后端 API 运行与端到端验证（阻塞项）

问题：本地启动后端时出现以下情况：

- `node --watch` 会触发 EMFILE（已改为不用 watch）。
- Fastify 在某环境监听 `0.0.0.0` 会报 `uv_interface_addresses`（已默认改为 `127.0.0.1`）。
- 端口冲突：旧的 8787 进程无法在当前权限限制下被 kill，导致新进程 EADDRINUSE。
临时措施：改用端口 8788 已能启动并监听（日志显示监听成功）。
但：我们还没稳定跑完“注册→登录→创建作品→列表作品”的脚本验证（上一次 `POST /api/works` 卡住，已给 DB 加 query/statement timeout + 事务日志）。

需要做：

- 确认 8788 的 `/api/health` 返回正常（再测一次）。
- 用 curl/脚本跑：register → me → create work → list works。
- 若 `POST /api/works` 仍卡：看后端日志（create work: begin/insert work/insert volume）卡在哪一步。

### B) 前端登录/登出 UI（未开始）

- 需要新增一个登录页（邮箱+密码），并在 UI 上有“登录/退出”入口。
- 会话方式：后端用 HttpOnly cookie（目前如此），前端 fetch 需要 `credentials: 'include'`。

### C) 云端存储接入（未开始）

当前前端数据仍走 IndexedDB：`src/storage/instance.ts` → `WritingStoreIndexedDB`。
需要新增一个 `WritingStoreCloud`（调用 `/api/*`）并在登录后切换。
建议先做最小：works/chapters/volumes（让写作正文能云端保存），其它（参考库/圣经）可后置。

---

## 关键文件清单

- 路由：`src/App.tsx`
- 全局壳（无右栏）：`src/components/AppShell.tsx`
- 写作全屏壳（带右栏）：`src/components/EditorShell.tsx`
- 写作页：`src/pages/EditorPage.tsx`
- 首页：`src/pages/HomePage.tsx`
- 样式：`src/index.css`
- DB 连接/迁移：`backend/db.js`、`backend/migrate.js`、`backend/test-connection.js`
- API：`backend/server.js`
- Vite 代理：`vite.config.ts`

---

## 继续推进的“下一步最短路径”

1. 先把后端在 8788 跑通（health + register + create work）。
2. 确认 `/api` 代理指向正确端口（8788）并重启前端 dev server。
3. 做前端登录页（最简单：一个 `/login` 页面 + 顶栏右侧显示登录状态）。
4. 实现 `WritingStoreCloud` 并切换（登录后云端、未登录本地）。

---

## 新窗口沟通模板（复制到新聊天窗口第一条消息）

我想继续做“注册/登录/云端数据存储”。请先阅读项目日志文件：`docs/开发日志-2026-04-02.md`。

当前状态：

- UI/路由/写作全屏壳已完成。
- VPS Postgres 已连通并建表（DB_SSL=false）。
- 后端 API 已写在 `backend/server.js`，但本地端到端还没跑通；旧 8787 进程无法 kill，临时用 8788。

你的任务：

1. 让后端在 8788 完整跑通：/api/health、/api/auth/register、/api/auth/me、POST/GET /api/works。
2. 将 Vite 代理 `/api` 指向 8788 并验证前端可访问后端。
3. 增加前端登录页 + 登录态显示/退出。
4. 新增 `WritingStoreCloud`（先实现 works/volumes/chapters），登录后切换到云端存储。