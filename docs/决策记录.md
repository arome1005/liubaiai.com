# 留白写作 — 决策记录

> **用途**：落实总体规划 **§11 步 1** 与 **第八章**：把待拍板项集中在一页，避免口头漂移。  
> **规则**：拍板后更新「状态」与「结论」；重大变更注明日期。  
> **执行进度**：见 `design/master-checklist.md`。

---

## 1. 第八章决策点（与总体规划对齐）


| #   | 议题                         | 状态      | 结论 / 备注                                                                                                                                                                             |
| --- | -------------------------- | ------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | 登录后默认落地页：`/` 还是 `/library` | 临时既定    | **在另行书面变更前**，登录成功默认跳转 `**/`**（见 `LoginPage`）；作品库路由仍为 `/library`。若未来统一为单一默认，再改路由与书签并更新本表。                                                                                            |
| 2   | 生辉 vs 推演内对话 vs 问策边界        | 已拍板（一期） | **生辉**只按纲出正文；**改纲与文策日志**在推演；**问策**（`/chat`）保留为 **开放式策略/拆书向多轮对话**，与写作侧栏（章内生成）并列；问策 **不**承担推演内定纲流水。实现见 **步 46**（`assemble-context` · `buildWenceChatSystemContent`）。若日后与推演日志联动，另开迭代项。 |
| 3   | 角色状态是否章节级快照、存表方式           | 已拍板（一期） | **一期 MVP**：每章 `chapter_bible.character_state`（`characterStateText`）自由文本备忘，装配器注入；不按「角色 id 横向表」强制建模。二期若需结构化快照或新表，另开迭代（见 §11 步 21、`docs/总体规划-改进版-2026-04.md`）。 |
| 4   | 健康度/密度指标一期是否展示             | 已拍板     | **一期不展示**；与总体规划 §7 一致，二期再评估                                                                                                                                                         |
| 5   | 第一期默认模型（厂商 + 模型 id）        | 暂定（工程） | **出厂默认**：`defaultAiSettings()` 中 `provider: "ollama"`（`src/ai/storage.ts`），便于本机无 Key 试跑；各厂商默认 `model` / `baseUrl` 随厂商文档迭代。**产品向「主推云端默认」**可另行改版，不阻塞一期发布。 |
| 6   | 第五章 ⭐ 体验项哪些进 P0/P1         | 待定（不阻塞） | 已落地部分见设置与侧栏（注入确认、本会话 token 上限等）；其余 **数字确认 / 长按 / 冷却** 等见总体规划 **§5.7**，随路线图迭代，**一期不强制单列 P0 表**。                                                                 |
| 7   | 多端冲突：是否仅 UI 提示、不自动合并       | 已对齐（一期） | **与现实现一致**：云同步与章节保存采用 `updatedAt` 乐观锁，冲突时 **仅 UI 提示**（`ChapterSaveConflictError`、写作页顶栏等），**不**自动合并正文；用户手动选择重载或合并导入路径。 |


---

## 2. 已文档化、无需重复拍板的产品定案

以下内容以 `docs/总体规划-路线图与导航整合.md` §3.4～3.5 为准（此处只索引）：

- **藏经**：本地书库；推演阶段提炼参考；生辉不直接消费藏经原文。
- **推演 → 定稿 → 生辉** 管线顺序。
- **作品标签**：多选可改；装配器后台注入 profile；UI 不展示内部技法全文与真人作家营销档位名。

---

## 3. 修订记录


| 日期         | 摘要                             |
| ---------- | ------------------------------ |
| 2026-04-05 | 初版：第八章七问表 + 定案索引               |
| 2026-04-05 | #4 健康度指标：已拍板一期不展示；修正 §2 文档链接笔误 |
| 2026-04-05 | #1 登录落地：记「临时既定」为 `/`，避免阻塞工程    |
| 2026-04-02 | #3 人物状态：与步 21 落地一致，一期拍板为章级 `character_state` 文本字段 |
| 2026-04-02 | #5 记工程默认 `ollama`；#6 标不阻塞；#7 与乐观锁冲突 UI 对齐 |

> **说明**：#5 已记 **工程出厂默认**；#6 仍为路线图项；#7 与 Hybrid 冲突 UI **已对齐**。若产品与工程默认不一致，以路线图修订为准。
