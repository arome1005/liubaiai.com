# Bug / 技术债 backlog（待讨论或分期）

> 与 `design/master-checklist.md` §D.4 互补：这里记 **需产品决策** 或 **工作量较大** 的项；修完可删或改划掉。  
> **规划级**：影响多模块一致性与发布验收的，见下节 **「规划级 / 分期」**，避免与零散 UI 混在同一表。

## 规划级 / 分期（影响总体规划或发布门禁）


| 项                   | 说明                                                                                                                                                         | 建议下一步                                                                             |
| ------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------- |
| ~~**全局弹层 z-index 分层**~~ | ~~已落地：`src/index.css` `:root` 定义 `--z-masthead` / `--z-modal-app-*` / `--z-command-*` / `--z-blocking-layer`；`WorkFormModal`、`GlobalCommandPalette`、`.inspiration-global-root`、`.command-palette-root` 等引用变量；极端组合仍建议避免同时打开多块弹层~~ | ~~与 §E 整页验收时目检即可~~ |
| ~~**步 49～54（第六章）**~~  | ~~子项已拆；**步 7** 上线验收已写入 `docs/生产环境部署.md`（Supabase Auth 等）；**步 54** 工程模板与 §7 表见 `docs/发布检查清单.md`~~                                                                                        | ~~发版前手测 §0～6 并填 §7 表（运维流程）~~                                |


## 待决策 / 大块功能


| 项                   | 说明                                                    | 建议下一步                                           |
| ------------------- | ----------------------------------------------------- | ----------------------------------------------- |
| **问策：跨设备会话 / 文策联动** | 当前仅 **sessionStorage** 按作品键；与推演文策日志双向引用未做             | 与步 33 后续或新表一并规划                                 |
| **写作页正文内 ⌘K**       | CodeMirror 为 `contenteditable`，全局快捷键有意不抢键，避免与编辑器快捷键冲突 | 若强需求：可选「仅当修饰键组合」或 CM 内自定义 keymap 打开面板（需评估与插件冲突） |


## 已处理（留档）


| 日期         | 项                      | 处理                                                                                     |
| ---------- | ---------------------- | -------------------------------------------------------------------------------------- |
| 2026-04-02 | `EditorShell` 渲染期写 ref | `commandOpenRef` 改为 `**useEffect` 同步**（满足 `react-hooks/refs`；Esc 与 ⌘K 逻辑不变）            |
| 2026-04-02 | 写作壳无命令面板               | `EditorShell` 挂载 `GlobalCommandPalette` + 顶栏「命令」+ 沉浸条入口；Esc 不与面板抢关侧栏（`commandOpenRef`） |
| 2026-04-02 | 步 46 问策 MVP            | `/chat` 对话页 + `buildWenceChat`*；决策记录 #2 一期拍板                                           |
